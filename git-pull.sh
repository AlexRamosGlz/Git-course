#!/env/bin bash

##	-- git pull -- 	  ##

git pull # hace lo mismo que el comando "fetch", sincroniza la branch remota, y despues intenta hacer merge automaticamente con nuestra branch local (origin/master -> master)


