#!/bin/bash

##    -- git rever --   ##

git revert HEAD # es usado como rollback, el comando no solo hace undo, sino que hace commit de los cambios del commit malo pero a la inversa

git revert 886e0a1 # hace rollback del commit especificado


